#include <stdio.h>

int main() {
    char name [50];
    int age;
    printf("enter your name")
    scanf("%s"&name);

    print f (nter your age );
    scanf ("%d",&age);

    printf ("\n hello %s! you are %d years old.\n",name,age);
    return 0;
}
